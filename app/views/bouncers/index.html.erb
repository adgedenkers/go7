<h1>Listing bouncers</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Url</th>
    <th>Chars</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @bouncers.each do |bouncer| %>
  <tr>
    <td><%= bouncer.title %></td>
    <td><%= bouncer.url %></td>
    <td><%= bouncer.chars %></td>
    <td><%= link_to 'Show', bouncer %></td>
    <td><%= link_to 'Edit', edit_bouncer_path(bouncer) %></td>
    <td><%= link_to 'Destroy', bouncer, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Bouncer', new_bouncer_path %>
